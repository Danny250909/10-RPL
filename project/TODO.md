# TODO - Perbaikan Portfolio (React + Vite)

- [ ] Audit masalah: pastikan benar-benar build `portfolio/` dipakai (bukan folder lain).
- [ ] Jalankan `npm install` dan `npm run build` di `c:/xampp/htdocs/10-RPL/portfolio`.
- [ ] Jalankan preview build (`npm run preview`) untuk cek tampilan.
- [ ] Perbaiki konfigurasi deploy bila perlu (mis. `base` di `vite.config.ts`, serta SPA routing).
- [ ] Pastikan link route (About/Projects/Contact) bekerja saat refresh.
- [ ] Update cache/build yang lama bila perlu.

